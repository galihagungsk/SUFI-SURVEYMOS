<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Izinkan akses folder /Android/data/<package>/files/ -->
    <external-files-path
        name="external_files_root"
        path="." />

    <!-- Izinkan akses folder /Android/data/<package>/files/Pictures/ -->
    <external-files-path
        name="external_pictures"
        path="Pictures/" />

    <!-- Opsional: cache kamera -->
    <cache-path
        name="camera_cache"
        path="." />

</paths>
